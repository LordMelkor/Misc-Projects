%function [output arg]=function name(input arg)
function [b, j] = Descsort(a)

%Function descsort sorts in the descending order, a real array 'a', and holds
%a permutation used to obtain array 'b' from the array 'a'.
[b,j] = sort(-a);
b = -b;