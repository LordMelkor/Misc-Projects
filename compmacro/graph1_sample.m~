
% Script file graph1_sample.

% Graph of the rational function y = x/(1+x^2).

n = 10;
x = linspace(-2,2,n);
y = x./(1+x.^2);
plot(x,y,'r')
title(sprintf('Graph 1.  Plot based upon n = %d points.', n))
axis([-2,2,-.8,.8])
xlabel('x')
ylabel('y')
grid
